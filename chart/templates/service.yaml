apiVersion: v1
kind: Service
metadata:
  name: {{ include "isotope-graph.fullname" . }}
  labels:
    {{- include "isotope-graph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isotope-graph.selectorLabels" . | nindent 4 }}
